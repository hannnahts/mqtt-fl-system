services:
  baseline-broker:
    image: eclipse-mosquitto:2
    container_name: baseline-broker
    ports:
      - "11883:1883"
    volumes:
      - ./brokers/baseline/mosquitto.conf:/mosquitto/config/mosquitto.conf
